{
  "customManagers": [
    {
      "customType": "regex",
      "managerFilePatterns": [
        "/^repo-tracker\\.ya?ml$/"
      ],
      "matchStrings": [
        "- (?<depName>[^\\s]+)\\s+(?<currentDigest>[a-f0-9]{40})(?:\\s+#(?<currentValue>[^\\s]+))?"
      ],
      "datasourceTemplate": "github-tags",
      "versioningTemplate": "semver"
    }
  ]
}
